# graph-view agent instructions

## Testing

- Always verify visual and interaction changes using the `playwright` skill before considering the task complete.
- For graph-view UI work, validate both appearance and behavior, including theme changes, pointer interactions, and other affected user flows.
- After making changes, run `bun run check` from the repository root.
