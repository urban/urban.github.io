---
"@apps/personal-website": patch
---

Move the default MDX element component map into the shared MDX runtime so
markdown links consistently render through the website's server-safe `MdxLink`
component.
