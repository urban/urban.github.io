---
"@urban/graph-view": patch
---

Remove the graph view background alpha setting so the canvas always renders with a solid theme background color, and mute selected nodes during hover when they fall outside the hovered node's immediate neighborhood.
