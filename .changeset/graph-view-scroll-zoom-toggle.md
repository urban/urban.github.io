---
"@urban/graph-view": patch
"@urban/website": patch
---

Add configurable scroll-wheel zooming to graph-view, expose external zoom controls from the bootstrap handle, disable wheel zoom for embedded vault graphs, and add demo controls for toggling scroll zoom in the graph-view HTML preview.
