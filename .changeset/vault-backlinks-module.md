---
"@urban/build-graph": patch
"@apps/personal-website": patch
---

Add a backlinks module to vault entry pages below the graph view, and export
`GraphSnapshotNoteNode` from the root `@urban/build-graph` entrypoint.
